[project]
name = "hackathon"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Oliver Sande", email = "oliversande@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "autogen-agentchat>=0.7.5",
    "autogen-ext[openai]>=0.7.5",
    "datasets>=5.0.0",
    "matplotlib>=3.10.9",
    "pydantic-settings>=2.14.1",
    "wikipedia>=1.4.0",
]

[project.scripts]
hackathon = "hackathon:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
